apiVersion: v2
name: quiz-backend
description: Helm chart for the DevOps Quiz backend API service

# Application chart for deploying:
# - Flask-based quiz API service
# - OpenAI API integration for question generation
# - External Secrets for SSM Parameter Store integration
# - TargetGroupBinding for ALB access
type: application

# Chart version - increment when making chart changes
version: 0.1.0

# Application version - Quiz App version
appVersion: "v1.0.23"
