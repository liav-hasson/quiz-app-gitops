---
# =============================================================================
# Quiz Frontend - ArgoCD Application
# =============================================================================
# Deploys the React-based quiz frontend
# Helm chart includes: deployment, service, TargetGroupBinding
# =============================================================================
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: quiz-frontend
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "40"  # Deploy after backend (wave 30)
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: quiz-frontend
    server: https://kubernetes.default.svc
  project: default
  source:
    path: quiz-frontend
    repoURL: https://github.com/liav-hasson/quiz-app-gitops.git
    targetRevision: main
    helm:
      valueFiles:
        - values.yaml
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
